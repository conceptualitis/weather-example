
module.exports = {
    // get: function (query) {
    //     var def = Q.defer();

    //     // take a location string and ask google for some coordinates
    //     // or lookup city info for that set of coordinates
    //     (function () {
    //         return query.location !== undefined ? geocoder.geocode(query.location) : geocoder.reverse(query.lat, query.long);
    //     })().then(function (res) {
    //             var coords = res[0],
    //                 // convert milliseconds to seconds
    //                 time = query.time || Math.floor(Date.now() / 1000);

    //             forecastio.timeMachine(coords.latitude, coords.longitude, time, function (err, data) {
    //                 var forecast = data;

    //                 forecast.city = coords.city;
    //                 forecast.state = coords.stateCode;

    //                 def.resolve(forecast);
    //             });
    //         }).catch(function (err) {
    //             console.log(err);
    //             def.reject(err);
    //         });

    //     return def.promise;
    // }
};
