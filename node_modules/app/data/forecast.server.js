var q = require('q'),
    request = require('superagent');

module.exports = {
    get: function (day) {
        var def = q.defer();

        request
            .get('http://localhost:8000/' + day)
            .set('Accept', 'application/json')
            .end(function (data) {
                console.log(datad);
                q.resolve(data);
            });

        return q.promise;
    }
};
