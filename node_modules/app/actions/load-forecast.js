var forecast = require('app/data/forecast.server');

module.exports = function (context, payload, done) {
    forecast.get(payload.day)
        .then(function (forecast) {
            context.dispatch('RECEIVE_FORECAST', forecast);
            done();
        });
};
