var React = require('react'),
    RouteHandler = require('react-router').RouteHandler,
    Nav = require('app/components/nav.jsx');

module.exports = React.createClass({
    render: function () {
        return (
            <div>
                <Nav />
                <RouteHandler context={ this.props.context } />
            </div>
        );
    }
});
